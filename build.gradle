apply plugin: 'cpp'

model {
    components {
        objslib(NativeLibrarySpec){
           sources {
              cpp {
                  source {
                      srcDir "./include"
                  }
              }
           }
	}
        main(NativeExecutableSpec) {
            binaries.all {
                lib library: "objslib"
            }
	}
    }
}
